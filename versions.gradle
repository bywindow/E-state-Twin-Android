/**
 * Shared file between builds so that they can all use the same dependencies and
 * maven repositories.
 **/

ext.deps = [:]
def versions = [:]
// TODO : Add libraries version

def buildVersions = [:]
buildVersions.minSdk = 24
buildVersions.targetSdk = 31
ext.buildVersions = buildVersions